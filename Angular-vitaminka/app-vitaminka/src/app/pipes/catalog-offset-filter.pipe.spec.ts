import { CatalogOffsetFilterPipe } from './catalog-offset-filter.pipe';

describe('CatalogOffsetFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CatalogOffsetFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
