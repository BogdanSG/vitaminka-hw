import { CatalogSortFilterPipe } from './catalog-sort-filter.pipe';

describe('CatalogSortFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CatalogSortFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
